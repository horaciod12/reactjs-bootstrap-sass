import React from "react";

const Header = () => {
  return (
    <header className="Header d-flex">
      <div className="Text container d-flex align-items-end mb-3">Gallery</div>
    </header>
  );
};

export default Header;
